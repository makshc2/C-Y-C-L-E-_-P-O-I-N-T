# Decisions — align-pages-with-figma

<!-- append-only; пише npx agent-orchestrator-kit handoff <name> з handoff.md ## Decisions -->

- 2026-09-15 Один change на всі 7 цілей — джерело дизайну одне, токени спільні; кожна сторінка стає окремим таском у `tasks.md`.
- 2026-09-15 Рівень точності: правка ТОКЕНІВ, не піксель-перфект. Критерій приймання — візуальна відповідність макету (палітра, типографічна шкала, spacing, ритм), а не попіксельний збіг. Відхилення в кілька пікселів прийнятні; невідповідність кольору/ваги шрифту/масштабу відступів — ні.
- 2026-09-15 Макет НЕ має auto-layout: у всіх вузлів `layout.mode = "none"` з абсолютними координатами на полотні 1728px. Отже макет не кодує адаптивну поведінку — переносяться пропорції та ритм, а не абсолютні px. Адаптивність лишається на наявних брейкпоінтах проєкту.
- 2026-09-15 Figma читається ОДИН раз — у фазі design; далі apply працює лише з `design-brief.md` і локальними PNG.
- 2026-09-15 Мобільних фреймів у макеті не виявлено на пробі — підтвердити під час повного збору в design.
- 2026-09-15 Q1 зсув вкладеного блоку під h1 (workshop x=250, army x=275, charity x=294 при h1 на x=149): навмисний прийом — «заголовок на gutter, вкладений блок з відступом», бо повторюється на 3 з 3 сторінок цього типу; там, де під h1 фото ряду, зсуву немає. Величина відступу (101/126/145) — недбалість макета: propose обирає ОДИН токен ≈ 100–128 px (напр. 104 або 120) для всіх трьох сторінок; таблиці — однакова ширина 1178 (≈ 68 %) на workshop і army.
- 2026-09-15 Мобільних фреймів у макеті немає (підтверджено кореневим запитом) — адаптивність лишається з наявних брейкпоінтів `1279px` / `767px`; переносяться пропорції (gutter 8.6 %, контент 83 %, таблиця 68 %) і ритм (крок 116 px на ≥1280, менший на брейкпоінтах — рішення propose).
- 2026-09-15 Джерело дизайну — лише файл `PVmxLvwhTXdblnuuAOhqkL`; старий ключ `5SDp4Vg9fHdWGWaOJAhxZ3` з архіву не використовується.
- 2026-09-15 Apply копіює в `src/assets/site/` лише два кропи (`fishky-card-photo.png`, `charity-qr.png`); 10 фото артефактів і GS-фото ідентичні наявним — не переекспортовувати; QA-скріни та `figma-nodes/*.json` у `src/` не класти.
- 2026-09-15 REST-дампи `assets/figma-nodes/*.json` — durable raw-джерело для точних текстів і координат; live Figma MCP у propose/review/apply заборонено.
- 2026-09-15 Хедер (`SiteHeader`) і сірі заголовки секцій — поза скоупом зміни; у макеті цього файлу їх немає, продукт рендерить за архівною спекою `figma-content-sync`.
- 2026-09-15 Див. `design.md` D0–D7 + D-Q9 — по одному явному рішенню з обґрунтуванням на кожне з Q2–Q9 (конспект вище в Done). Токен Q1 (`decisions.md`) конкретизовано як `--content-indent: 120px`.
- 2026-09-16 Нових рішень цієї сесії немає. Q2–Q9 (`design.md` D1–D7/D-Q9) НЕ відкривались заново — reviewer підтвердив, що обґрунтування узгоджене з proposal.md/tasks.md, без суперечностей і без scope creep відносно Non-goals.
- 2026-09-16 Нових design-рішень (Q2–Q9) не приймалось і не відкривалось заново. Єдина нова письмова нотатка — уточнення біля D5 в `design.md` (не зміна рішення, а обґрунтування вибору між `padding`/`min-height` і `<p>`/`<h2-h3>`, вже описане вище в Done). Finding #4 (gap 177px для charity) — нове число, відсутнє в попередніх design-рішеннях (D7 його не згадував), але взяте прямо з design-brief.md і позначене reviewer'ом як незначне/низькоризикове; `design.md` D7 навмисно не чіпався для цього, оскільки finding #4 некритичний і не вимагав перегляду рішення.
- 2026-09-16 Нових design-рішень (Q2–Q9) цієї сесії не приймалось і не відкривалось заново — review підтвердив межу непорушеною. Єдиний новий факт для фіксації: 2026-09-16 review раунд 2 завершено вердиктом APPROVE (після REQUEST CHANGES раунду 1); зміна формально готова до `/opsx:apply`. Одне неблокуюче спостереження зафіксоване в `review.md`/`apply-notes.md`: task 5.2 не задає `grid-row` для нових `.gs__setup`/`.gs__how` — CSS Grid auto-placement коректно розмістить їх у неявний row 3 (DOM-порядок після `.gs__photo`/`.gs__copy`), дії не потребує.
- 2026-09-16 Нових design-рішень (Q2–Q9) у цій фазі не приймалось — apply реалізовував затверджений контракт `tasks.md` дослівно. Зафіксовані рішення фази:
- 2026-09-16 2026-09-16 Розподіл роботи: `design-implementer` спавнився рівно на 2 pixel-таски (5.2, 7.2) за рекомендацією handoff; решта 9 тасків — parent-driven, без `code-writer`, бо кожен є детермінованою текстовою правкою з дослівним Do.
- 2026-09-16 2026-09-16 Одинарні `\n` у `body1[2]`/`body1[3]` (`goldenSprintsDetail`) НЕ отримали `white-space: pre-line` — див. «Open observations» нижче. Свідоме утримання: додавання CSS-правила поза `Do` таска 5.2 було б рішенням поза межею Q2–Q9, що заборонено constraints цієї сесії.
- 2026-09-16 2026-09-16 Конвенція рендеру `\n` у проєкті підтверджена по коду: `ArtifactRow.vue:54` і `SiteFooter.vue:53` мають `white-space: pre-line`; `design-brief.md` рядок 601 документує її ж для `/artifacts`. Тобто task 2.1 (`\n\n` в артефактах) рендериться коректно — `ArtifactRow` уже має `pre-line`.
