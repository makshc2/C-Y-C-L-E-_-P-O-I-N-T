## 0. Спільні токени

- [x] 0.1 Додати спільні CSS-токени ритму й відступу
  Files: src/styles/tokens.css
  Do: У блоці `:root` після рядка `--gutter: 149px;` додай п'ять нових custom properties: `--content-indent: 120px;` (горизонтальний відступ вкладеного блоку від h1/gutter), `--section-pad-y: 116px;` (вертикальний крок хедер→h1, блок→блок, блок→низ секції), `--sheet-w: 68%;` (ширина плейсхолдера Google Sheet на workshop/army), `--fishky-photo-w: 507px;` і `--fishky-photo-h: 810px;` (фото-бокс деталки Фішок, окремо від `--gs-photo-w/h`). Потім зміни значення двох існуючих властивостей: `--charity-qr-w: 566px;` → `--charity-qr-w: 255px;` і `--charity-qr-h: 528px;` → `--charity-qr-h: 238px;`. Не змінюй `--gs-photo-w`, `--gs-photo-h`, `--artifact-photo-w`, `--artifact-photo-h`, `--gutter`, `--charity-green-h`, `--army-stripe-h`, `--section-h-home`.
  Done-when: `src/styles/tokens.css` містить рядки `--content-indent: 120px;`, `--section-pad-y: 116px;`, `--sheet-w: 68%;`, `--fishky-photo-w: 507px;`, `--fishky-photo-h: 810px;`, `--charity-qr-w: 255px;`, `--charity-qr-h: 238px;`; рядок `--gs-photo-w: 516px;` лишається без змін.

## 1. /workshop — вертикальний ритм і відступи

- [x] 1.1 Звести ритм і відступи майстерні до макета
  Files: src/pages/WorkshopPage.vue
  Do: У `<style scoped>` зміни `.workshop{padding:142px var(--gutter) 40px}` на `padding: var(--section-pad-y) var(--gutter) var(--section-pad-y)`; додай `margin-left: var(--content-indent)` до `.workshop__contact` і зміни його `gap: 16px` на `gap: 32px`; додай `margin-left: var(--content-indent)` до `.workshop__sheet` і зміни його `margin-top: 16px` на `margin-top: var(--section-pad-y)`; зміни `.workshop__sheet-frame{width:60%}` на `width: var(--sheet-w)`; у наявному блоці `@media (max-width: 1279px)` додай `margin-left: 0` для `.workshop__contact` і `.workshop__sheet` та поверни `.workshop__contact{gap:16px}`, зберігши наявні `padding: 32px 16px 48px` на `.workshop`; блок `@media (max-width: 767px)` не чіпай. Не змінюй `WORKSHOP_PRICE_SHEET_SRC`, прив'язку `:src` iframe, і `workshopCopy.heading`/`workshopCopy.instagramContact`.
  Done-when: `WorkshopPage.vue` містить `var(--section-pad-y)` для верхнього й нижнього padding `.workshop` і для `margin-top` `.workshop__sheet`, `var(--content-indent)` для `margin-left` `.workshop__contact` і `.workshop__sheet`, `var(--sheet-w)` для `.workshop__sheet-frame`, і `gap: 32px` для `.workshop__contact`; блок `@media (max-width: 1279px)` містить `margin-left: 0`; `WORKSHOP_PRICE_SHEET_SRC` імпорт і `@load="markReady"` лишаються; `npm run typecheck` завершується з кодом 0.

## 2. /artifacts — розділювачі абзаців і дві правки копі

- [x] 2.1 Виправити розділювачі абзаців і два слова в текстах артефактів
  Files: src/content/artifacts.ts
  Do: У масиві `artifacts` зміни поле `text` рівно цих дев'яти елементів, не чіпаючи `id`, `tone` і елемент `colnago`: (1) `crest` — заміни одинарний `\n` перед «Цей архів радості» на подвійний `\n\n`; (2) `colossi` — заміни обидва наявні `\n` (перед «Colossi — невелика майстерня» і перед «Сьогодні ця рама») на `\n\n`; (3) `harry-hall` — встав НОВИЙ розрив `\n\n` між «...як заведено у фіксед-культурі.» і «Юрчик багато їздив» (зараз одне речення без розриву) і заміни наявний `\n` перед «Тепер фрейм висить» на `\n\n`; (4) `danylo` — заміни обидва наявні `\n` (перед «Рама постраждала» і перед «Тепер вона») на `\n\n`; (5) `kokkedal` — встав НОВИЙ розрив `\n\n` між «...і грає в байк-поло.» і «Для гри», і встав слово «він» так, щоб вийшло «Для гри він замовив раму у бренду Kokkedal» (подвійний пробіл у «другом  нашого простору» лишається без змін); (6) `bag` — заміни обидва наявні `\n` (перед «Востаннє ця сумка» і перед «Після того, як вони приїхали») на `\n\n`; (7) `gemu` — заміни весь однопараграфний `text` на цей точний п'ятирядковий текст, з'єднаний одинарними `\n` без порожніх рядків: "У просторі Велоточки зберігається особливий духовний артефакт — футболка від львівської велоспільноти GEMU, один із учасників якої, Ілля «Панч» Руднікович, загинув, захищаючи Україну у лавах ЗСУ.\nGEMU - це фіксерська велоспільнота зі Львова, активна як в тилу так і у лавах армії. Коли Ілля вступив до армії, друзі з GEMU виготовили патріотичні футболки на підтримку та надіслали одну з них йому. На привеликий жаль, він її не встиг отримати. \nПісля загибелі Іллі футболку передали його батькам, які подарували її спільноті, щоб вона жила як символ памʼяті про Панча та його спільноту.\nМи створили з цієї футболки інсталяцію: помістили її у велосипедний обід, доповнили військовими патчами від наших друзів із різних бригад, розміщуємо поруч QR-коди з актуальними зборами для підтримки армії.\nЦя інсталяція - це не просто предмети. Це шана до вибору, памʼять  про ціну свободи і вдячність за подвиги." (трейлінг-пробіл після «отримати.» і подвійний пробіл у «памʼять  про» лишаються дослівно); (8) `helmets` — заміни обидва наявні `\n` на `\n\n`; (9) `lucas` — заміни обидва наявні `\n` на `\n\n`. Не змінюй `ArtifactRow.vue`, `ArtifactsPage.vue`, жоден з 10 PNG-файлів, і не чіпай `colnago`.
  Done-when: `artifacts.ts` містить підрядок `\n\n` у полях `text` елементів `crest`, `colossi`, `harry-hall`, `danylo`, `kokkedal`, `bag`, `helmets`, `lucas`; елемент `kokkedal` містить підрядок «Для гри він замовив»; елемент `gemu` містить рівно 4 символи `\n` і 0 входжень `\n\n`; елемент `colnago` не містить `\n`; `npm run typecheck` завершується з кодом 0.

## 3. /projects — третя картка «Фішки велодоріжки»

- [x] 3.1 Додати третю картку до переліку проєктів
  Files: src/pages/ProjectsPage.vue, new file: src/assets/site/fishky-card-photo.png
  Do: Якщо файла `src/assets/site/fishky-card-photo.png` ще немає — скопіюй його байт-у-байт з `openspec/changes/align-pages-with-figma/assets/fishky-card-photo.png` (новий файл; пікселі не перегенеровувати). У `ProjectsPage.vue` імпортуй `fishkyCardPhoto` з `@/assets/site/fishky-card-photo.png` і `fishkyVelodorizhky` з `@/content/fishkyVelodorizhky` (модуль уже експортує `title` і `paragraphs`). Після наявного другого `<div class="projects__rule" />` (зараз останній вузол шаблону) додай третій `<ProjectRow :photo="fishkyCardPhoto" photo-alt="Фішки велодоріжки" :title="fishkyVelodorizhky.title" to="/projects/fishky-velodorizhky"><p>{{ fishkyVelodorizhky.paragraphs[0] }}</p></ProjectRow>`, використовуючи дефолтний бокс фото компонента (178×235, `object-fit: cover`, без кастомного класу ширини на кшталт `.projects__army`). Четвертий `.projects__rule` після нового ряду не додавай. Не змінюй наявні два `<ProjectRow>`, їхні імпорти чи CSS `.projects__army`.
  Done-when: `src/assets/site/fishky-card-photo.png` існує і не порожній; `ProjectsPage.vue` містить рівно три `<ProjectRow` і рівно два `<div class="projects__rule" />`; третій `<ProjectRow>` має `to="/projects/fishky-velodorizhky"`; `npm run typecheck` завершується з кодом 0.

## 4. /projects/fishky-velodorizhky — нове фото і відступи

- [x] 4.1 Замінити фото деталки Фішок і звести відступи
  Files: src/pages/FishkyVelodorizhkyPage.vue, new file: src/assets/site/fishky-card-photo.png, src/styles/tokens.css
  Do: Якщо файла `src/assets/site/fishky-card-photo.png` ще немає — скопіюй його байт-у-байт з `openspec/changes/align-pages-with-figma/assets/fishky-card-photo.png` (новий файл; старий `fishky-velodorizhky-photo.png` не видаляти). Переконайся, що в `tokens.css` є `--fishky-photo-w: 507px;` і `--fishky-photo-h: 810px;` (додані в 0.1; додай, якщо відсутні). У `FishkyVelodorizhkyPage.vue` зміни імпорт фото з `@/assets/site/fishky-velodorizhky-photo.png` на `@/assets/site/fishky-card-photo.png`; онови атрибути `<img>` `width`/`height` з `516`/`682` на `507`/`810`. У `<style scoped>` зміни `grid-template-columns: var(--gs-photo-w) minmax(0, 786px)` на `grid-template-columns: var(--fishky-photo-w) minmax(0, 786px)`, `column-gap: 133px` на `column-gap: 138px`, `.fishky__photo{width:var(--gs-photo-w);height:var(--gs-photo-h)}` на `width: var(--fishky-photo-w); height: var(--fishky-photo-h)`, і `.fishky__title{margin:0 0 72px}` на `margin: 0 0 63px`. Не змінюй `GoldenSprintsPage.vue`, `--gs-photo-w`, `--gs-photo-h`, back-лінк, чи текст чотирьох абзаців.
  Done-when: `src/assets/site/fishky-card-photo.png` існує і не порожній; `FishkyVelodorizhkyPage.vue` імпортує `fishky-card-photo.png` (не `fishky-velodorizhky-photo.png`), його `<img>` має `width="507" height="810"`, стилі містять `var(--fishky-photo-w)`, `var(--fishky-photo-h)`, `column-gap: 138px` і `margin: 0 0 63px` для `.fishky__title`; `GoldenSprintsPage.vue` не змінений; `npm run typecheck` завершується з кодом 0.

## 5. /projects/golden-sprints — новий копірайт і другий ряд

- [x] 5.1 Переписати контент-модуль деталки Golden Sprints
  Files: src/content/projects.ts
  Do: Заміни експорт `goldenSprintsDetail` (зараз `{title, paragraphs: string[2]}`) на об'єкт такої форми: `title: 'Golden sprints'`; `body1` — масив з 4 рядків: `'Проєкт, народжений велоспільнотою, в стінах CyclePоint.'`, `'У міжсезоння холодна пора року та негода часто ставлять велосипедні заїзди на паузу. Для нас велосипедний сезон ніколи не закінчується — він просто змінює локаці і формат.'`, `'Цей проєкт народився взимку, у стінах CyclePoint як відповідь на нудне зимове міжсезоння. Команда велоентузіастів, які хотіли зберегти пульс спільноти, драйв дружніх батлів та адреналін навіть тоді, коли за вікном сніг або сльота. \nМи взяли класичну культуру Goldsprint — динамічних велосипедних перегонів на стаціонарних станках — і переосмислили її у цифровому форматі, зробивши відкритою для кожного.'`, `"Унікальність продукту: мінімум громіздкої апаратури, максимум мобільності\nБільшість цифрових платформ для тренувань вимагають важких, дорогих смарт-станків вартістю в тисячі доларів, спеціального софту та прив'язки до розетки. Ми пішли іншим шляхом - мобільність, доступність, зручність\nНаш веб-додаток працює на будь-якому сучасному мобільному пристрої через звичайний браузер. Жодних складних інсталяцій, проводів чи громіздких екранів."` (одинарний `\n` усередині блоків 3 і 4 лишається одинарним рядком, не подвійним; кирилична «о» в «CyclePоint» і «локаці» без «ї» — дослівно); `setup` — об'єкт `{heading: 'Усе, що потрібно для старту перегонів:', steps: ['Твій велосипед ', 'Базовий велостанок або прості тренувальні ролики.', 'Компактний Bluetooth-датчик ', 'Cмартфон, планшет, лептоп'], note: 'Підключив через Bluetooth в один клік — і ти вже на старті дистанції.', whereHeading: 'Де ганяти?', whereItems: ['Вдома у вітальні з сім’єю чи друзями', "У CyclePoint - приходь на наші зимові заїзди, щоб відчути підтримку і змагатися пліч-о-пліч.", "У кав’ярні, барі чи на вечірці: Завдяки повній мобільності наш сетап розгортається за 5 хвилин у будь-якій локації."], cta: 'Почни заїзд прямо зараз!'}` (трейлінг-пробіли в перших двох `steps` і латинська «C» в «Cмартфон» — дослівно); `howItWorks` — об'єкт `{heading: 'Як це працює:', steps: [{title: '01. Підключись', text: 'Закріпи бездротові датчики на задні втулки колес та увімкни Bluetooth на телефоні.'}, {title: '02. Відкрий додаток', text: 'Заходь на нашу сторінку і підключайся до ПО.'}, {title: '03. Тисни на всі 100%', text: 'Обирай дистанцію спринту, крути на максимумі, змагайся з другом !'}]}` (пробіл перед «!» — дослівно). Видали поле `paragraphs`.
  Done-when: `content/projects.ts` експортує `goldenSprintsDetail.body1` (масив з 4 елементів), `goldenSprintsDetail.setup.cta === 'Почни заїзд прямо зараз!'`, `goldenSprintsDetail.howItWorks.steps` (масив з 3 об'єктів `{title, text}`); поля `paragraphs` більше немає; `npm run typecheck` виявить усі місця використання старої форми (наступний таск 5.2 їх виправляє).

- [x] 5.2 Перебудувати шаблон і сітку деталки Golden Sprints
  Files: src/pages/GoldenSprintsPage.vue
  Do: Прибери імпорт `readMoreLabel` (більше не використовується на цій сторінці). Заміни `<p v-for="(paragraph, index) in goldenSprintsDetail.paragraphs">` на `<p v-for="(paragraph, index) in goldenSprintsDetail.body1" class="gs__body1">`; додай правило `.gs__copy .gs__body1{margin-bottom:2em}` (вища специфічність за наявний `.gs__copy p{margin:0 0 1em}`, дає подвійний інтервал між блоками body1). Перейменуй клас `.gs__more` (разом з його `:focus-visible`-правилом) на `.gs__cta`, зміни `RouterLink` текст з `{{ readMoreLabel }}` на `{{ goldenSprintsDetail.setup.cta }}`, і зміни його типографіку з `var(--text-read-more)`/`var(--font-weight-light)` на `var(--text-body)`/`var(--font-weight-regular)`. Додай новий другий ряд під наявним: лівий блок `<div class="gs__setup" style="grid-column:1">` з (у порядку) `<p class="gs__setup-heading">{{ goldenSprintsDetail.setup.heading }}</p>`, `<ol>` з `setup.steps`, `<p>{{ goldenSprintsDetail.setup.note }}</p>`, `<p class="gs__setup-heading">{{ goldenSprintsDetail.setup.whereHeading }}</p>`, `<ul>` з `setup.whereItems`, і перенесений сюди `.gs__cta` RouterLink; правий блок `<div class="gs__how" style="grid-column:2">` з `<p class="gs__how-heading">{{ goldenSprintsDetail.howItWorks.heading }}</p>` і `<ul>` з `howItWorks.steps`, де кожен `<li>` показує `step.title` і `step.text`. Класи `gs__setup-heading` і `gs__how-heading` застосовуй рівно на тег `<p>` (НЕ `<h2>`/`<h3>`/інший заголовковий тег) і не додавай для них власного `font-size`/`font-weight`: design-brief.md документує ці рядки як частину суцільного 20px Regular текстового блоку, тож типографіка успадковується від спільного правила `.gs__setup p, .gs__how p, .gs__setup li, .gs__how li` (20px Regular `#e7fc84`: `var(--text-project-body)` + `var(--font-weight-regular)` + `var(--color-green)`, як у `.gs__copy p`). Обидва нові блоки `.gs__setup` і `.gs__how` отримують `margin-top: var(--section-pad-y)`. У `.gs` зміни `grid-template-columns: var(--gs-photo-w) minmax(0, 786px)` на `grid-template-columns: 645px minmax(0, 786px)` і `column-gap: 133px` на `column-gap: 0`; додай `.gs__photo{justify-self:start}` (фото лишається `width: var(--gs-photo-w)` = 516px, флеш-ліво в ширшій 645px-колонці). Зміни `.gs__title{margin:0 0 72px}` на `margin: 0 0 63px`. У `.gs` зміни `padding: 34px 148px 0 145px` на `padding: 34px 148px var(--section-pad-y) 145px` (нижній відступ секції стає реальним padding, а не побічним ефектом `min-height`) і видали рядок `min-height: 1251px` повністю (застарілий: калібрований під старий короткий контент; з новим другим рядом і явним нижнім padding він більше не потрібен і не відповідає реальній висоті нового вмісту); рядок `min-height: 0` у блоці `@media (max-width: 767px)` для `.gs` лишається без змін. У `@media (max-width: 767px)` розшир наявне правило `max-width: none` на нові елементи `.gs__setup`/`.gs__how` (список/абзаци); наявний `display:flex;flex-direction:column` на `.gs` вже стековує нові блоки в порядку DOM, додаткового мобільного CSS для порядку не потрібно. Не змінюй back-лінк, фото-імпорт чи розмір фото (516×682 лишається).
  Done-when: `GoldenSprintsPage.vue` не імпортує `readMoreLabel`; шаблон містить `goldenSprintsDetail.setup.cta`, прив'язаний до `RouterLink` з `to="/projects/golden-sprints/app"`; шаблон містить рівно два `<p class="gs__setup-heading">` (для `setup.heading` і `setup.whereHeading`) і рівно один `<p class="gs__how-heading">`, і жодного `<h2>`/`<h3>`/`<h4>` усередині `.gs__setup`/`.gs__how`; стилі не задають окремого `font-size`/`font-weight` для класів `.gs__setup-heading`/`.gs__how-heading`; стилі містять `grid-template-columns: 645px minmax(0, 786px)`, `column-gap: 0`, `margin: 0 0 63px` для `.gs__title`, `padding: 34px 148px var(--section-pad-y) 145px` для `.gs`, і НЕ містять рядка `min-height: 1251px`; `npm run typecheck` і `npm run build` завершуються з кодом 0.

## 6. /projects/army-support — back-лінк і розташування таблиці

- [x] 6.1 Звести колір back-лінка і ритм армії до макета
  Files: src/pages/ArmySupportPage.vue, src/components/site/SiteBackLink.vue
  Do: У `SiteBackLink.vue` зміни `.site-back--lime{color:var(--color-fg)}` на `color: var(--color-section-title)` (токен уже дорівнює `rgba(30,30,30,0.4)` у `tokens.css`; `tone="lime"` використовується лише на `ArmySupportPage.vue`, інші сторінки не зачіпаються). У `ArmySupportPage.vue` `<style scoped>` зміни `.army{padding:142px var(--gutter) 40px}` на `padding: 34px var(--gutter) var(--section-pad-y)`; зміни `.army :deep(.site-back){margin:0 0 16px}` на `margin: 0 0 54px`; зміни `.army__sheet{margin-top:16px}` на `margin-top: var(--section-pad-y)` і додай `margin-left: var(--content-indent)`; зміни `.army__sheet-frame{width:100%}` на `width: var(--sheet-w)`; у наявному блоці `@media (max-width: 1279px)` додай `margin-left: 0` для `.army__sheet`. Не змінюй `CHARITY_RESULTS_SHEET_SRC`, прив'язки `:src`/`:title` iframe, чи `armySupportCopy.heading`.
  Done-when: `SiteBackLink.vue` містить `.site-back--lime` з `color: var(--color-section-title)`; `ArmySupportPage.vue` стилі містять `padding: 34px var(--gutter) var(--section-pad-y)` для `.army`, `margin: 0 0 54px` для back-лінка, `margin-top: var(--section-pad-y)` і `margin-left: var(--content-indent)` для `.army__sheet`, `width: var(--sheet-w)` для `.army__sheet-frame`; імпорт `CHARITY_RESULTS_SHEET_SRC` не змінений; `npm run typecheck` завершується з кодом 0.

## 7. /charity — три смуги з контентом

- [x] 7.1 Створити контент-модуль благодійності
  Files: new file: src/content/charity.ts
  Do: Створи файл, що експортує `charityCopy`: `heading: 'Крутимо велокультуру разом!'`; `qrCaption: 'Разовий донат Велоточці'`; `lead: 'Велоточка — це більше, ніж майстерня, пиво та кава. Це точка перетину велолюдей, ідей і міського руху. Ми створюємо живий простір та втілюємо різноманітні проєкти: від благодійних ініціатив і соціальних заходів до кінопоказів, фестивалів та предметної популяризації велоруху.'`; `donationImpact: { heading: 'Твій донат допомагає:', items: ['Втілювати та масштабувати в життя наші міські, культурні та благодійні проєкти', 'Підтримувати більше спільнот у їх соціальних ідеях ', 'Тримати громадський інструмент і насос завжди готовими та доступними для кожного райдера'], closing: 'Будь-який внесок — це додаткова швидкість для наших спільних ініціатив.' }` (трейлінг-пробіл у другому пункті — дослівно); `contacts: { intro: 'Хочеш долучитися не лише фінансово?', body: ['Ми завжди відкриті до спільних ідей, волонтерства, колаборацій та партнерств! ', 'Якщо маєш бажання допомогти часом, руками чи експертизою — пиши нам: '], emailPrefix: 'cyclepoint.kyiv@gmail.com або  ', instagramLabel: '@cyclepoint_kyiv', closing: 'Знайдемо спільну дистанцію!' }` (трейлінг-пробіли і подвійний пробіл перед «@cyclepoint_kyiv» — дослівно). Значення типізуй як `as const`.
  Done-when: `src/content/charity.ts` існує, експортує `charityCopy` з полями `heading`, `qrCaption`, `lead`, `donationImpact.items.length === 3`, `contacts.instagramLabel === '@cyclepoint_kyiv'`; `npm run typecheck` завершується з кодом 0.

- [x] 7.2 Перебудувати сторінку благодійності на три смуги
  Files: src/pages/CharityPage.vue, src/assets/site/charity-qr.png
  Do: Перезапиши `src/assets/site/charity-qr.png` байт-у-байт файлом `openspec/changes/align-pages-with-figma/assets/charity-qr.png` (заміна байтів наявного файлу, пікселі не перегенеровувати). У `CharityPage.vue` імпортуй `charityCopy` з `@/content/charity` (створений у 7.1) і `EVENTS_INSTAGRAM_URL` з `@/constants/site` (повторне використання наявної константи, нову не заводь). Заміни наявний шаблон (`.charity__green` лише з QR, порожній `.charity__violet`) на три секції: (A) лайм — `<h1>{{ charityCopy.heading }}</h1>`, QR-блок (`<img :src="qrUrl" width="255" height="238" alt="QR-код для донату Велоточці">` + `<p>{{ charityCopy.qrCaption }}</p>` по центру на 8px нижче QR) з `margin-left: var(--content-indent)`, і `<p>{{ charityCopy.lead }}</p>` праворуч від QR-блоку: обгорни QR-блок і лід у спільний контейнер `display:flex; align-items:flex-start; gap:177px` (значення з design-brief.md: x=726 ліда мінус правий край QR 549 = 177px) і задай ліду `max-width: 856px`; (B) violet — `<h2>{{ charityCopy.donationImpact.heading }}</h2>`, `<ul>` з `donationImpact.items`, `<p>{{ charityCopy.donationImpact.closing }}</p>`, повна ширина, 32px Regular `#d5f252` (`var(--text-contact-title)` + `var(--font-weight-regular)` + `var(--color-accent-lime)`); (C) лайм — `<h2>{{ charityCopy.contacts.intro }}</h2>`, `<p>` для кожного `contacts.body`, один `<p>`, що поєднує `{{ charityCopy.contacts.emailPrefix }}` і `<a :href="EVENTS_INSTAGRAM_URL" target="_blank" rel="noopener noreferrer">{{ charityCopy.contacts.instagramLabel }}</a>` (підкреслений), і `<p>{{ charityCopy.contacts.closing }}</p>`, повна ширина, 32px Light `#1e1e1e` (`var(--text-contact-title)` + `var(--font-weight-light)` + `var(--color-fg)`). Кожна секція отримує `padding: var(--section-pad-y) var(--gutter)`. Припини використовувати `--charity-green-h` і `--section-h-home` у цьому файлі (визначення токенів у `tokens.css` не видаляй). Форму оплати чи бронювання не додавай.
  Done-when: `CharityPage.vue` містить три секції з `charityCopy.heading`, `charityCopy.donationImpact.heading`, `charityCopy.contacts.intro`; імпортує `EVENTS_INSTAGRAM_URL` з `@/constants/site`; `<a>` для «@cyclepoint_kyiv» має `:href="EVENTS_INSTAGRAM_URL"`; `<img>` QR має `width="255" height="238"`; флекс-контейнер смуги A містить `gap: 177px`, а `<p>{{ charityCopy.lead }}</p>` має `max-width: 856px`; на сторінці немає елемента `<form>`; `npm run typecheck` завершується з кодом 0.

## 8. Валідація

- [x] 8.1 Typecheck і build
  Files: package.json
  Do: Запусти `npm run typecheck`, після успішного завершення запусти `npm run build`.
  Done-when: Обидві команди завершуються з кодом 0.
